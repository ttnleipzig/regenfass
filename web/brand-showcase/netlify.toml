# Netlify site: set Base directory to `web/brand-showcase` in the Netlify UI.
[build]
  command = "cd ../.. && corepack enable && pnpm install && pnpm --filter @ttnleipzig/regenfass-brand-showcase build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
