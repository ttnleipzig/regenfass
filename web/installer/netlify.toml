# Netlify site: set Base directory to `web/installer` in the Netlify UI.
[build]
  command = "cd ../.. && corepack enable && pnpm install && pnpm --filter @ttnleipzig/regenfass-installer build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
