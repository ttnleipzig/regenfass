import type { Component } from "solid-js";
import type { FormProps } from "../types";

/** Placeholder: full welcome form UI lives in the main installer flow; kept for `stateToFormMap` imports. */
export const StepStartWaitingForUserForm: Component<FormProps> = () => (
	<div data-testid="StepStartWaitingForUserForm-stub" />
);
