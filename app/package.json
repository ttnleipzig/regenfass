{
	"name": "regenfass-tailwind",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"sync": "browser-sync start --server --files \"./src/*\"",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "npx tailwindcss -i src/css/styles.css -o build/css/styles.css --watch",
		"build": "npx tailwindcss -i src/css/styles.css -o build/css/build.css --minify"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@tailwindcss/forms": "^0.5.7",
		"browser-sync": "^3.0.2"
	}
}
