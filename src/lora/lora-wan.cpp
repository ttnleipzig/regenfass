#include <Arduino.h>

namespace Lora
{
    namespace Wan
    {
        void setup()
        {
            if (DEBUG_LEVEL > 0)
                Serial.println("Setup:\tLora WAN");
        };
        void loop(){};
    }
}
